# tests/e2e/test_complete_cv_flow.py
"""
E2E tests for the complete CV flow.

These tests will be implemented once the router endpoints
are connected to real use cases instead of mock data.
"""

import pytest


@pytest.mark.e2e
def test_placeholder():
    """Placeholder to prevent pytest from failing on empty test directory."""
    pytest.skip("E2E tests pending: endpoints are still stubs with mock data")
